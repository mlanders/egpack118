# Test Database Configuration
# Create a separate database or branch on Neon for testing
# Example: postgresql://username:password@host/test_database?sslmode=require

TEST_DATABASE_URL=postgresql://username:password@host/egpack118_test?sslmode=require

# Or use your main database URL for now (not recommended for production)
# TEST_DATABASE_URL=${DATABASE_URL}

# Better Auth Configuration (Test)
BETTER_AUTH_SECRET=test-secret-key-for-testing
BETTER_AUTH_URL=http://localhost:5173
